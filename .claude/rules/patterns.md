# Design Patterns

> Preferred design patterns for this project.
> Extends common patterns.md with project-specific patterns.

## Pattern: Repository Pattern

**When**: Data access needs encapsulation behind a consistent interface.

```
[Example implementation for your language/framework]
```

## Pattern: Observer / Event Pattern

**When**: Decoupling components that need to react to state changes.

```
[Example implementation for your language/framework]
```

## Pattern: State Machine

**When**: An entity has well-defined states with explicit transitions.

```
[Example implementation for your language/framework]
```

## Pattern: Strategy Pattern

**When**: An algorithm's behavior should be selectable at runtime.

```
[Example implementation for your language/framework]
```

## Pattern: Command Builder

**When**: Building complex commands or queries from configuration.

```
[Example implementation for your language/framework]
```

## Anti-Patterns (Avoid)

- God objects (single class/module doing everything)
- Deep inheritance hierarchies (prefer composition)
- Singleton abuse (prefer dependency injection)
- Magic numbers (use named constants)

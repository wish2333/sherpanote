"""Plugin runtime subsystem for optional document extraction backends.

Provides subprocess-isolated execution of optional backends (docling,
opendataloader-pdf) in a dedicated plugin virtual environment.
"""
